<%= form_for([@playlist, @playlistsongs]) do |f| %>
  <% if @playlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
      <% @playlist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :songs %><br>
    <%= f.collection_select(:song_id, Song.all, :name, :id, {prompt: true, :include_black=>true}) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

