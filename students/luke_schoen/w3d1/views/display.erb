<%# name the result rows in main.rb route go through each row of the result %>

<%# content_for :menu_sub do %>

<div id="display_brief">
    <h2>Display Brief:</h2><br>

    <a href="/?sort_by=<%=@sort_type%>">Order By <%= @sort_type %></a>

	<p> 
		<table>
			<tr>
				<th>ID</th>
				<th>Title</th>
				<th>Abstract</th>
				<th>Created Date</th>
				<th>Details</th>
			</tr>
	<% @res.each do |row| %>
			<tr>
				<td>
		 	<%= row['id'] %>
		 		</td>
		 		<td> 
			<%= row['title'] %> 
				</td>
				<td>
			<%= row['abstract'] %> 
				</td>
				<td>
			<%= row['created_at'] %> 
				</td>
				<td>
			<!--<form method='get' action="/posts/<%#= row['id'] %>">
				<button>Show</button>
			</form>-->
			<a href="/posts/<%= row['id'] %>">Show</a>
				</td>
			</tr>
	<% end %>
		</table>

	</p>

</div>

<%# end %>
