<%= form_for @painting do |f| %> 

	<%= f.label :title %> 
	<%= f.text_field :title %><br>

	<%= f.label :year %>
	<%= f.text_field :year %><br>

	<%= f.label :medium %>
	<%= f.text_field :medium %><br>

	<%= f.label :style %>
	<%= f.text_field :style %><br>

	<%= f.label :image %>
	<%= f.text_field :image %><br>

	<%= f.label :artist %>
	<%= f.text_field :artist_id %><br>

	<%= f.label :artist %>
	<%= f.collection_select :artist_id, Artist.all, :id, :name, :include_blank => true, prompt: true %><br>

<!--	<select>
		<option></option>
		<option></option>
		<option></option>
		<option></option>
		<option value="1">Sam</option> where the id is 1
	</select>
-->

	<%= f.submit  %>

<% end %>